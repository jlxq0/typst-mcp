# Canonical source for the vendored templates.
#
# Templates live in the OfficeMaster repo alongside each brand's brief and its
# Word/PowerPoint masters. They are vendored here (rather than cloned at image
# build) so the build needs no credentials, images stay hermetic, and drift shows
# up as a reviewable diff instead of two builds of the same tag differing.
#
# To update: edit the canonical copy, then re-run scripts/sync-templates.sh.

repo = git@forge.oddie.app:thehansogroup/OfficeMaster.git
commit = 4c99d8ca66677f965930c4d2ef77da7f040a35c6
hanso = brands/hanso/typst/hanso.typ
ksc = brands/ksc/typst/ksc.typ
lenno = brands/lenno/typst/lenno.typ
freudenberg = brands/freudenberg/typst/freudenberg.typ
synced = 2026-08-22
